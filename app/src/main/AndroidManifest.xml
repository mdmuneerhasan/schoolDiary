<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.multi.schooldiary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".teacher.AttendanceRegisterActivity"></activity>
        <activity android:name=".teacher.ManageClassActivity" />
        <activity android:name=".teacher.AttendanceActivity" />
        <activity android:name=".principal.TeacherRequestActivity" />
        <activity android:name=".student_or_guardian.StudentGuardianActivity" />
        <activity android:name=".student_or_guardian.RequestActivity" />
        <activity android:name=".teacher.StudentRequestActivity" />
        <activity android:name=".teacher.TeacherActivity" />
        <activity android:name=".teacher.GuardianRequestActivity" />
        <activity android:name=".principal.PrincipalActivity" />
        <activity android:name=".activity.SetDefaultActivity" />
        <activity
            android:name=".activity.SignUpActivity"
            android:label="@string/title_activity_sign_up" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>